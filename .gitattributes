# 统一按 LF 存储，避免跨系统协作出现整文件「假 diff」
* text=auto eol=lf

# 批处理文件必须是 CRLF。cmd.exe 解析 LF 换行的 .bat 时，多行 if(...) 块会
# 出错并直接退出，表现为「双击后窗口一闪而过」。这条规则写在最前面。
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资源不做换行转换
*.png binary
*.jpg binary
*.ico binary
*.sqlite3 binary

